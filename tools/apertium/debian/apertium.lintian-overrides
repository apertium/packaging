# Apertium wants /usr/share/apertium/modes/README to exist in that folder, because it pertains directly to the contents.
package-contains-documentation-outside-usr-share-doc

# The warning "name expected (got '\c'): treated as missing" is a false positive. The .Lk syntax is correct.
groff-message
